# 2.6.9 asynchat.py
# 2.6 added:
# return_stmt ::= ret_expr RETURN_END_IF come_from_pop
def initiate_send(self):
    while self:
        if self:
            x = 'a'
        else:
            del self.producer_fifo[0]
        return
